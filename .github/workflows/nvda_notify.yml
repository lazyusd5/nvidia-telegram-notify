name: NVDA Hourly Alert (Market Hours)

on:
  schedule:
    - cron: "0 * * * *"  # ทุกชั่วโมง
  workflow_dispatch:      # สามารถกด Run เองได้

jobs:
  nvda_notify:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install yfinance matplotlib pytz requests

      - name: Run NVDA Notify Script
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: python nvda_notify.py
